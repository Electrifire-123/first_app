import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

// here we will write what to display and where to display

const root = ReactDOM.createRoot(document.getElementById('root'));
root .render(
  <React.StrictMode>
    <App/>
  </React.StrictMode>
)